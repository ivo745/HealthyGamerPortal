
@model HealthyGamerPortal.Common.ViewModels.Guild.DeleteGuildItemViewModel

<modal-form size="Large" title="Delete a Guild">
    <form asp-action="Delete" id="validate-form">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        @Html.HiddenFor(m => m.Id)
        <div class=row>
            <form-entry asp-for="Name" input-type="Text" read-only="true"  column-width="12"></form-entry>
        </div>
    </form>
</modal-form>